{
  "name": "groupmatch-root",
  "version": "1.0.0",
  "description": "GroupMatch Fullstack Setup",
  "scripts": {
    "dev:backend": "cd backend && .\\venv\\Scripts\\python -m uvicorn app.main:app --reload --port 8000",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "npm i concurrently -D && concurrently \"npm run dev:backend\" \"npm run dev:frontend\""
  }
}
